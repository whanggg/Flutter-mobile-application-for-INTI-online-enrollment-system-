import 'package:flutter/material.dart';

// ✅ Primary Theme Colors
const primaryColor = Color(0xFF0056D2);  // Deep Blue (for buttons & highlights)
const secondaryColor = Color(0xFF00AEEF); // Light Blue (for accents & highlights)
const backgroundColor = Color(0xFFF5F7FA); // Light Grayish-Blue (for the main background)

// ✅ AppBar & Navigation Colors
const appBarColor = Color(0xFF003366);  // Dark Blue (for strong contrast)
const tabColor = Color(0xFF007ACC);  // Bright Blue (for active tabs)

// ✅ Text Colors
const textColor = Color(0xFF212121);  // Dark Gray (for primary text)
const secondaryTextColor = Color(0xFF616161); // Medium Gray (for subtitles & secondary text)
const hintTextColor = Color(0xFF9E9E9E);  // Light Gray (for placeholder text)

// ✅ Card & Container Colors
const cardColor = Colors.white;  // White (for clean card UI)
const containerColor = Color(0xFFE3F2FD);  // Soft Blue (for slight contrast)

// ✅ Buttons & Interactive Elements
const buttonColor = Color(0xFF007BFF);  // Bright Blue (for CTA buttons)
const buttonTextColor = Colors.white;  // White (to ensure readability on buttons)
const disabledButtonColor = Color(0xFFB0BEC5);  // Muted Blue-Gray (for disabled buttons)

// ✅ Form Field & Input Colors
const inputFieldColor = Color(0xFFF0F4F8); // Very Light Gray-Blue (for form fields)
const inputBorderColor = Color(0xFF90A4AE);  // Subtle Blue-Gray (for input borders)
const focusedBorderColor = Color(0xFF42A5F5); // Bright Blue (for active input fields)

// ✅ Dividers & Borders
const dividerColor = Color(0xFFBDBDBD); // Light Gray (for separating sections)

// ✅ Status & Notification Colors
const successColor = Color(0xFF28A745);  // Green (for success messages)
const warningColor = Color(0xFFFFC107);  // Yellow (for warnings)
const errorColor = Color(0xFFDC3545);  // Red (for errors)
const infoColor = Color(0xFF17A2B8);  // Teal (for information messages)

// ✅ Search Bar & Chat Box Colors (if applicable)
const searchBarColor = Color(0xFFE0E0E0);  // Light Gray (for search fields)
const chatBoxColor = Color(0xFF90CAF9);  // Light Blue (for chat interfaces)

